// Change these values to change the number of attempts, the size of the field and the words' length
const COUNT_OF_TRIES = 6;
const COUNT_OF_LETTERS = 5;

const GAME_RELEASE_DATE = new Date(2023, 0, 7);

export { COUNT_OF_TRIES, COUNT_OF_LETTERS, GAME_RELEASE_DATE };
